---

interface Props {
    containerStyles: string
}

const { containerStyles } = Astro.props;
---

<section class={`grid grid-cols-2 items-center p-8 w-full rounded-2xl ${containerStyles}`} >
		<slot name="first-col"/>	
		<slot name="second-col"/>	
</section>